#Escriba un script de Bash que imprima en pantalla la cantidad de archivos del directorio /var/log/SSERVICE 
#que contienen un patrón de texto que el usuario pasa como parámetro,
#SERVICE es una variable de entomo, si la misma no posee ningún valor debe tomar el valor por defecto "local service".
#El script debe finalizar retornando el valor que indica que el proceso se ejecutô correctamente.
